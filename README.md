Script makes mapping columns from source CSV with columns in template CSV.

Command line to perform mapping:

<pre><code>convert_table.py —-source <source CSV> —-template <template CSV> —-target <target CSV>
</code></pre>

